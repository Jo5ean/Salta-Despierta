---
const aboutData = {
  description: "SW Tucumán Aventura es una empresa de turismo activo, prestadora de servicios en la naturaleza, comprometida con el uso saludable y sustentable del ambiente. La misión de SW Tucumán Aventura es brindar servicios de turismo activo; de índole educativo, deportivo, recreacional y empresarial; acompañado de una gran responsabilidad social en relación al uso de nuestro ambiente.",
  secondaryText: "Para ello utilizamos los recursos de la naturaleza, en armonía con actividades de aventura, en los alrededores del Dique Celestino Gelsi; el cual se encuentra ubicado en la comuna El Cadillal, en la Provincia de Tucumán."
};
---
<div class="bg-[#F2F2F2] relative overflow-visible">
  <div class="container mx-auto px-4 md:px-24 lg:px-24 2xl:px-4" id="about">
    <div class="grid grid-cols-1 lg:grid-cols-2 min-h-screen items-center">
      <!-- Columna izquierda - Título grande -->
      <div class="relative z-10">
        <div class="flex flex-col slide-in">
          <h1 class="text-[#E26128] text-5xl sm:text-7xl 2xl:text-8xl font-titles tracking-titles leading-none relative">
            <div class="overflow-visible">
              <span class="translate-effect block relative z-10">QUIENES</span>
              <span class="translate-effect block relative z-10">Somos</span>
            </div>
          </h1>
        </div>
      </div>

      <!-- Columna derecha - Contenido -->
      <div class="lg:pl-12 py-8 relative z-0">
        <div class="space-y-6">
          <h2 class="text-[#005F73] text-2xl font-bold mb-4">S.W. Tucuman Aventuras</h2>
          <p class="text-[#2E2E2E] text-lg sm:text-xl leading-relaxed">
            {aboutData.description}
          </p>
          <p class="text-[#2E2E2E] text-lg sm:text-xl leading-relaxed">
            {aboutData.secondaryText}
          </p>
          <div class="mt-8">
            <a 
              href="#video" 
              class="inline-flex items-center px-6 py-3 bg-[#E26128] text-white rounded-md hover:bg-[#E28928] transition-colors duration-300"
            >
              <span class="mr-2">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM9.555 7.168A1 1 0 008 8v4a1 1 0 001.555.832l3-2a1 1 0 000-1.664l-3-2z" clip-rule="evenodd" />
                </svg>
              </span>
              Ver video
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .slide-in {
    overflow: visible;
  }

  .translate-effect {
    opacity: 0;
    transform: translateX(-100%);
    transition: all 0.8s ease-out;
    position: relative;
    z-index: 10;
  }

  .translate-effect.visible {
    opacity: 1;
    transform: translateX(0);
  }
</style>

<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        observer.unobserve(entry.target);
      }
    });
  }, {
    threshold: 0.1
  });

  document.querySelectorAll('.translate-effect').forEach((el) => {
    observer.observe(el);
  });
</script>
